server:
  port: 8888
spring:
  application:
    name: config-server

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/microcent/spring-cloud-simple.git
          searchPaths: cloud-config-repo